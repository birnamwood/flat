$("#reload-comment-area").html("<%= j(render partial: 'posts/post_comments', collection: @comments, as: "comment") %>");
$("#reload-pagination-area").html("<%= j(paginate @comments, class: "paginate") %>");