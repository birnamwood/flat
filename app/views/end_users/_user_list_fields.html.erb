 <table>
  <thead>
    <tr>
      <td>アイコン</td>
      <td>ユーザー名</td>
      <td>在住</td>
      <td>投稿数</td>
      <td>登録日</td>
    </tr>
  </thead>
  <tbody>
    <% users.each do |user| %>
      <tr>
          <% if user.icon_image_id.present? %>
        <td><%= image_tag(user.icon_image_id.to_s, size: "100x100") %></td>
          <% else %>
        <td><%= image_tag("no_image.png", size: "100x100") %></td>
          <% end %>
        <td><%= link_to user.nickname, end_user_path(user) %></td>
        <td><%= user.prefecture.prefecture_name %></td>
        <td><%= user.posts.count %></td>
        <td><%= user.created_at.strftime("%Y年%m月%d日") %><td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= paginate users, class: "paginate" %>